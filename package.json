{
  "name": "task-board-mcp",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "bun server.ts",
    "test": "bun test",
    "tw:debounce-metrics": "bun run bin/tw-debounce-metrics.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.82.0",
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^6.0.2"
  }
}
