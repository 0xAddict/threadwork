# Roadmap — {SPRINT_NAME}

End-to-end autonomous build. One sprint, {N} coordinated work items, hard spec-gate at ≥98% of acceptance criteria passing.

## Goal

{One-paragraph WHY — what the user is actually trying to achieve. Keep this user-facing so a future reader (or the Generator) can sanity-check that the criteria still serve the goal.}

## Work items

### W1 — {short name} ({TDD or smoke})

{What gets delivered. Where it lives. Any pre-existing structure to extend. The shape of the change. Concrete examples of the new content. If TDD: spell out the test first, then the implementation.}

### W2 — {short name}

{Same shape.}

### W3 — {short name}

{Same shape.}

## Boundaries

- Do NOT {a specific thing the Generator might be tempted to do but shouldn't}.
- Do NOT regenerate or rewrite from scratch — only the surgical edits described above.
- Do NOT touch {adjacent system that should be left alone}.

## Sprint count

ONE sprint. Hard gate at ≥98% acceptance criteria passing. If failed, monitor unsticks and retries up to 5 ticks before escalating.
