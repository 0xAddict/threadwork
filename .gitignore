# Secrets
.env
*.token
access.json

# SQLite runtime files
*.db
*.db-shm
*.db-wal
*.db.bak
*.db.bak-shm
*.db.bak-wal
*.db.fresh

# Token-bearing DB backups / recovery dumps (HARD GUARDRAIL — never commit)
*.bak
tasks*.bak
tasks*.bak-shm
tasks*.bak-wal
tasks.db.bak-*
tasks.db.backup-*
recovery/
state/*.txt

# Lock files
locks/

# Dependencies
node_modules/
bun.lockb

# OS
.DS_Store

# Image assets / mockups
*.png

# Logs
*.log

# Briefings (generated at runtime)
briefings/*.json

# Telegram inbox media
inbox/
logs/
.claude/scheduled_tasks.lock

# Hook + bin backups (created by install.sh when overwriting existing files)
hooks/*.bak-*
bin/*.bak-*

# Skill placeholder backups (not canonical)
skills/**/*.placeholder-bak-*
*.placeholder-bak-*

# system/ backup namespace — guard against accidental secret/runtime leakage.
# (Curated structure files are added explicitly; these patterns block the rest.)
system/**/*.bak
system/**/*.bak-*
system/**/*.db
system/**/*.db-shm
system/**/*.db-wal
system/**/*.log
system/secrets.env
system/**/.env
system/**/*.token
system/**/access.json

# transient caches / config backups / archives (backup-push hygiene, Gwei TG 7392)
.fastembed_cache/
mcp.json.bak-*
.harness-archive-*/
