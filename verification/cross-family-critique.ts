// verification/cross-family-critique.ts — P7 Cross-Family Critique module.
//
// STAGE 0 STUB. Implemented stage-by-stage per build-p7/PLAN.md:
//   EPIC-01 taxonomy (ModelFamily + resolvers), EPIC-02 evaluator core
//   (evaluateCrossFamily + CrossFamilyVerdict), EPIC-03 P6 read-only adapter
//   (consumes getFailureClassifications(db, filter?)), EPIC-04 persistence
//   (cross_family_critiques table + persistCrossFamilyCritique local
//   BEGIN IMMEDIATE + read predicates), EPIC-05 getCrossFamilyCritiques P8
//   read contract. Additive, flag-gated (cross_family_critique_enabled),
//   default-OFF. Zero edits to decision.ts / verify.ts /
//   failure-classification.ts (consumed read-only).
export {}
