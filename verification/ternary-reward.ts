// verification/ternary-reward.ts — P8 ternary rewards (Stage 0 scaffold stub).
//
// GREENFIELD module. Terminal rung of the verification axis
// (P6 typed failure classification -> P7 cross-family critique -> P8
// ternary rewards). This Stage-0 stub exists only so tsconfig.p8.json has a
// real file to typecheck; EPIC-01 (Stage 1) and EPIC-02 (Stage 2) replace
// this with the real vocabulary + pure evaluator.
//
// P8 does NOT touch decision.ts, failure-classification.ts, or
// cross-family-critique.ts — see PLAN.md Overlap boundaries.

export const TERNARY_REWARD_STAGE0_STUB = true
