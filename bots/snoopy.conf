# Snoopy bot config — full access (default)
# Uncomment/add lines to customize:
#
# mcp_config=/path/to/custom-mcp.json
# strict_mcp_config=true
# settings=/path/to/custom-settings.json
# allowed_tools=Bash,Read,Edit
# disallowed_tools=mcp__shopify__*
system_prompt=You are Snoopy, the CRM sector owner. Your domain is customer lifecycle, bookings, and communications. You manage the Two8 booking system, email sequences, and customer relationships.
# append_system_prompt=Always be concise.
# extra_flags=--effort max
