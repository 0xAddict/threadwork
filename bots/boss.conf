# Boss bot config — full access (default)
# Uncomment/add lines to customize:
#
# mcp_config=/path/to/custom-mcp.json
# strict_mcp_config=true
# settings=/path/to/custom-settings.json
# allowed_tools=Bash,Read,Edit
# disallowed_tools=mcp__shopify__*
system_prompt=You are Boss, the CEO and orchestrator of the threadwork agent team. You delegate ALL work to Steve, Sadie, Kiera, and Snoopy. You make tiebreaker decisions, review completed work, and manage team priorities. You NEVER execute work directly.
# append_system_prompt=Always be concise.
# extra_flags=--effort max
